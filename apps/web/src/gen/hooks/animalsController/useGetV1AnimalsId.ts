/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import useSWR from "swr";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import type {
	GetV1AnimalsIdQueryResponse,
	GetV1AnimalsIdPathParams,
} from "../../models/animalsController/GetV1AnimalsId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getV1AnimalsId } from "../../clients/animalsController/getV1AnimalsId.ts";

export const getV1AnimalsIdQueryKey = (id: GetV1AnimalsIdPathParams["id"]) =>
	[{ url: "/v1/animals/:id", params: { id: id } }] as const;

export type GetV1AnimalsIdQueryKey = ReturnType<typeof getV1AnimalsIdQueryKey>;

export function getV1AnimalsIdQueryOptions(
	id: GetV1AnimalsIdPathParams["id"],
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1AnimalsIdQueryKey(id);
	return queryOptions<
		GetV1AnimalsIdQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1AnimalsIdQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1AnimalsId(id, { ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @summary Get animal by ID
 * {@link /v1/animals/:id}
 */
export function useGetV1AnimalsId<
	TData = GetV1AnimalsIdQueryResponse,
	TQueryData = GetV1AnimalsIdQueryResponse,
	TQueryKey extends QueryKey = GetV1AnimalsIdQueryKey,
>(
	id: GetV1AnimalsIdPathParams["id"],
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetV1AnimalsIdQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getV1AnimalsIdQueryKey(id);

	const query = useQuery(
		{
			...getV1AnimalsIdQueryOptions(id, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
