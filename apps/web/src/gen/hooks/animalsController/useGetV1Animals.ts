/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import useSWR from "swr";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import type {
	GetV1AnimalsQueryResponse,
	GetV1AnimalsQueryParams,
} from "../../models/animalsController/GetV1Animals.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getV1Animals } from "../../clients/animalsController/getV1Animals.ts";

export const getV1AnimalsQueryKey = (params?: GetV1AnimalsQueryParams) =>
	[{ url: "/v1/animals" }, ...(params ? [params] : [])] as const;

export type GetV1AnimalsQueryKey = ReturnType<typeof getV1AnimalsQueryKey>;

export function getV1AnimalsQueryOptions(
	params?: GetV1AnimalsQueryParams,
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1AnimalsQueryKey(params);
	return queryOptions<
		GetV1AnimalsQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1AnimalsQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1Animals(params, {
				...config,
				signal: config.signal ?? signal,
			});
		},
	});
}

/**
 * @description Returns a paginated list of animals for the organization. Supports filtering by status, species and sex.
 * @summary List animals
 * {@link /v1/animals}
 */
export function useGetV1Animals<
	TData = GetV1AnimalsQueryResponse,
	TQueryData = GetV1AnimalsQueryResponse,
	TQueryKey extends QueryKey = GetV1AnimalsQueryKey,
>(
	params?: GetV1AnimalsQueryParams,
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetV1AnimalsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getV1AnimalsQueryKey(params);

	const query = useQuery(
		{
			...getV1AnimalsQueryOptions(params, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
