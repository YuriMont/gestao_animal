/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	QueryKey,
	QueryClient,
	UseSuspenseQueryOptions,
	UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
	GetV1AnimalsIdQueryResponse,
	GetV1AnimalsIdPathParams,
} from "../../models/animalsController/GetV1AnimalsId.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getV1AnimalsId } from "../../clients/animalsController/getV1AnimalsId.ts";

export const getV1AnimalsIdSuspenseQueryKey = (
	id: GetV1AnimalsIdPathParams["id"],
) => [{ url: "/v1/animals/:id", params: { id: id } }] as const;

export type GetV1AnimalsIdSuspenseQueryKey = ReturnType<
	typeof getV1AnimalsIdSuspenseQueryKey
>;

export function getV1AnimalsIdSuspenseQueryOptions(
	id: GetV1AnimalsIdPathParams["id"],
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1AnimalsIdSuspenseQueryKey(id);
	return queryOptions<
		GetV1AnimalsIdQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1AnimalsIdQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1AnimalsId(id, { ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @summary Get animal by ID
 * {@link /v1/animals/:id}
 */
export function useGetV1AnimalsIdSuspense<
	TData = GetV1AnimalsIdQueryResponse,
	TQueryKey extends QueryKey = GetV1AnimalsIdSuspenseQueryKey,
>(
	id: GetV1AnimalsIdPathParams["id"],
	options: {
		query?: Partial<
			UseSuspenseQueryOptions<
				GetV1AnimalsIdQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey =
		resolvedOptions?.queryKey ?? getV1AnimalsIdSuspenseQueryKey(id);

	const query = useSuspenseQuery(
		{
			...getV1AnimalsIdSuspenseQueryOptions(id, config),
			...resolvedOptions,
			queryKey,
		} as unknown as UseSuspenseQueryOptions,
		queryClient,
	) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
