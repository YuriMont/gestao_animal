/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	QueryKey,
	QueryClient,
	UseSuspenseQueryOptions,
	UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
	GetV1AnimalsQueryResponse,
	GetV1AnimalsQueryParams,
} from "../../models/animalsController/GetV1Animals.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getV1Animals } from "../../clients/animalsController/getV1Animals.ts";

export const getV1AnimalsSuspenseQueryKey = (
	params?: GetV1AnimalsQueryParams,
) => [{ url: "/v1/animals" }, ...(params ? [params] : [])] as const;

export type GetV1AnimalsSuspenseQueryKey = ReturnType<
	typeof getV1AnimalsSuspenseQueryKey
>;

export function getV1AnimalsSuspenseQueryOptions(
	params?: GetV1AnimalsQueryParams,
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1AnimalsSuspenseQueryKey(params);
	return queryOptions<
		GetV1AnimalsQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1AnimalsQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1Animals(params, {
				...config,
				signal: config.signal ?? signal,
			});
		},
	});
}

/**
 * @description Returns a paginated list of animals for the organization. Supports filtering by status, species and sex.
 * @summary List animals
 * {@link /v1/animals}
 */
export function useGetV1AnimalsSuspense<
	TData = GetV1AnimalsQueryResponse,
	TQueryKey extends QueryKey = GetV1AnimalsSuspenseQueryKey,
>(
	params?: GetV1AnimalsQueryParams,
	options: {
		query?: Partial<
			UseSuspenseQueryOptions<
				GetV1AnimalsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey =
		resolvedOptions?.queryKey ?? getV1AnimalsSuspenseQueryKey(params);

	const query = useSuspenseQuery(
		{
			...getV1AnimalsSuspenseQueryOptions(params, config),
			...resolvedOptions,
			queryKey,
		} as unknown as UseSuspenseQueryOptions,
		queryClient,
	) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
