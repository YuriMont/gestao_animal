/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import useSWR from "swr";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
	QueryKey,
	QueryClient,
	QueryObserverOptions,
	UseQueryResult,
} from "@tanstack/react-query";
import type { GetV1FinancialSummaryQueryResponse } from "../../models/financialController/GetV1FinancialSummary.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getV1FinancialSummary } from "../../clients/financialController/getV1FinancialSummary.ts";

export const getV1FinancialSummaryQueryKey = () =>
	[{ url: "/v1/financial/summary" }] as const;

export type GetV1FinancialSummaryQueryKey = ReturnType<
	typeof getV1FinancialSummaryQueryKey
>;

export function getV1FinancialSummaryQueryOptions(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1FinancialSummaryQueryKey();
	return queryOptions<
		GetV1FinancialSummaryQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1FinancialSummaryQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1FinancialSummary({
				...config,
				signal: config.signal ?? signal,
			});
		},
	});
}

/**
 * @description Get a financial summary for the organization. Returns aggregated financial statistics including total income, expenses, available funds, and other key metrics. Useful for quick overview of financial health.
 * @summary Get financial summary
 * {@link /v1/financial/summary}
 */
export function useGetV1FinancialSummary<
	TData = GetV1FinancialSummaryQueryResponse,
	TQueryData = GetV1FinancialSummaryQueryResponse,
	TQueryKey extends QueryKey = GetV1FinancialSummaryQueryKey,
>(
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetV1FinancialSummaryQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getV1FinancialSummaryQueryKey();

	const query = useQuery(
		{
			...getV1FinancialSummaryQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
