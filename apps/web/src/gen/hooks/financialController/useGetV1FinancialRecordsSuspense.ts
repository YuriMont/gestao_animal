/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	QueryKey,
	QueryClient,
	UseSuspenseQueryOptions,
	UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type { GetV1FinancialRecordsQueryResponse } from "../../models/financialController/GetV1FinancialRecords.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getV1FinancialRecords } from "../../clients/financialController/getV1FinancialRecords.ts";

export const getV1FinancialRecordsSuspenseQueryKey = () =>
	[{ url: "/v1/financial/records" }] as const;

export type GetV1FinancialRecordsSuspenseQueryKey = ReturnType<
	typeof getV1FinancialRecordsSuspenseQueryKey
>;

export function getV1FinancialRecordsSuspenseQueryOptions(
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getV1FinancialRecordsSuspenseQueryKey();
	return queryOptions<
		GetV1FinancialRecordsQueryResponse,
		ResponseErrorConfig<Error>,
		GetV1FinancialRecordsQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getV1FinancialRecords({
				...config,
				signal: config.signal ?? signal,
			});
		},
	});
}

/**
 * @description Retrieve financial records for an organization. Returns all financial records including payments, expenses, receipts, and other financial activity. Useful for auditing and financial analysis.
 * @summary Get financial records
 * {@link /v1/financial/records}
 */
export function useGetV1FinancialRecordsSuspense<
	TData = GetV1FinancialRecordsQueryResponse,
	TQueryKey extends QueryKey = GetV1FinancialRecordsSuspenseQueryKey,
>(
	options: {
		query?: Partial<
			UseSuspenseQueryOptions<
				GetV1FinancialRecordsQueryResponse,
				ResponseErrorConfig<Error>,
				TData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey =
		resolvedOptions?.queryKey ?? getV1FinancialRecordsSuspenseQueryKey();

	const query = useSuspenseQuery(
		{
			...getV1FinancialRecordsSuspenseQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as UseSuspenseQueryOptions,
		queryClient,
	) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
		queryKey: TQueryKey;
	};

	query.queryKey = queryKey as TQueryKey;

	return query;
}
