/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/axiosInstance";
import type {
	Client,
	RequestConfig,
	ResponseErrorConfig,
} from "@/lib/axiosInstance";
import type {
	GetV1AnimalsQueryResponse,
	GetV1AnimalsQueryParams,
} from "../../models/animalsController/GetV1Animals.ts";

function getGetV1AnimalsUrl() {
	const res = { method: "GET", url: `/v1/animals` as const };
	return res;
}

/**
 * @description Returns a paginated list of animals for the organization. Supports filtering by status, species and sex.
 * @summary List animals
 * {@link /v1/animals}
 */
export async function getV1Animals(
	params?: GetV1AnimalsQueryParams,
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<
		GetV1AnimalsQueryResponse,
		ResponseErrorConfig<Error>,
		unknown
	>({
		method: "GET",
		url: getGetV1AnimalsUrl().url.toString(),
		params,
		...requestConfig,
	});
	return res.data;
}
